# Python deps for the Boogu-Image web UI wrapper.
# NOTE: install PyTorch first with the CUDA build for your machine, e.g. (CUDA 12.6):
#   pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 \
#       --index-url https://download.pytorch.org/whl/cu126
#
# flash-attn (Linux + Ampere/Ada GPUs only) gives a large speedup — install the official
# prebuilt wheel that matches your torch/cuda/python/abi, e.g. for torch2.7/cu12/py3.12:
#   pip install "https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3.post1/flash_attn-2.8.3.post1+cu12torch2.7cxx11abiTRUE-cp312-cp312-linux_x86_64.whl"
# (Windows/Turing have no flash-attn; the model falls back to PyTorch SDPA automatically.)

torch>=2.7.1,<2.12
torchvision>=0.22.1
torchaudio>=2.7.1
diffusers[torch]>=0.35.2,<0.39
transformers[torch]>=4.57.3,<6
accelerate>=1.0
torchao>=0.15,<0.18
cache-dit>=1.3,<2
kernels>=0.14,<0.15
einops>=0.7
numpy>=1.26
pillow>=10
scipy>=1.11
webdataset>=1.0,<2
python-dotenv>=1.0,<2
omegaconf>=2.3,<3
huggingface_hub>=0.34
gradio>=5
